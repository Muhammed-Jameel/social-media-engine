# Optional Postiz integration. The plugin never auto-loads this file.
# Supply real values privately through your host or process environment.
POSTIZ_API_URL=https://your-postiz-instance.example/api/public/v1
POSTIZ_API_KEY=
# Keep disabled; enable only for a specific explicitly approved external write.
SOCIAL_ENGINE_ALLOW_PUBLISH=false
