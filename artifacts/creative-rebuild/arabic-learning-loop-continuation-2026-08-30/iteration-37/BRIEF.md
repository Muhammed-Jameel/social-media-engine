# Iteration 37 — Rebuild: A Result Needs Provenance

## Objective

Replace iteration 16's sparse source reminder with an authored Arabic visual system in which source, version, and update date register together before a result resolves into knowledge.

## Exact copy

- Metadata: `مبدأ ثقة · ٢٨`
- Headline: `النتيجة بلا مصدر` / `ليست معرفة`
- Registration layers: `مصدر البيانات` / `إصدارها` / `تاريخ تحديثها`
- Footnote: `١ — سجّل مصدر البيانات، إصدارها، وتاريخ تحديثها.`

## Content grounding

NIST AI 600-1 recommends determining data origin and content lineage and documenting upstream sources for provenance. The Arabic remains a deliberately narrower operational principle, not a claim of compliance.

## Concept tournament

1. **Selected — provenance registration:** three offset `معرفة` layers align around source, version, and update-date bands, borrowing the logic of print registration without borrowing a reference composition.
2. Rejected — magnifying glass over a citation: literal iconography would reduce provenance to inspection.
3. Rejected — linked-node lineage diagram: repeats process-diagram behavior and looks like a generic data slide.
4. Rejected — archive folder with stamped fields: simulated paperwork would turn trust into administrative cosplay.
5. Rejected — oversized footnote marker in empty space: repeats the original's sparse editorial-slide failure.

## Composition constraints

- 1080×1350, Arabic-only live overlay, RTL, 68 px safe margin.
- Dark full-canvas field; bright registration accents; canonical horizontal logo.
- `معرفة` is the central image and remains legible through controlled outline offsets.
- Source, version, and update date are large enough to survive 324×405.
- No mock page, database icon, magnifying glass, chain diagram, interface, external asset, generated image, or professional reference pixel.

## Professional principles studied

- Arabic Typography Posters: Arabic lettering can hold an entire poster silhouette when phrase and form are treated as one decision.
- Ta Magazine: editorial hierarchy depends on active dialogue among type, space, and structure rather than isolated text blocks.

References informed principles only. No spread, grid, letterform treatment, phrase composition, palette, asset, or project pixel was reused.

## Evidence boundary

The visual teaches documentation behavior. It does not claim provenance alone guarantees accuracy, truth, safety, compliance, or fitness for use. `ليست معرفة` is an editorial trust principle, not a formal definition.

## Quality target

At least 88/100 under `EXTRAORDINARY-RUBRIC.md`, no dimension below 8, Arabic/mobile/evidence at least 9, no hard fail, and publication disabled.
