# Iteration 16 — A Result Needs Provenance

## Objective

Teach an AI/data-governance principle: a result is not usable knowledge when its source and currency cannot be traced.

## Exact copy

- Metadata: `مبدأ ثقة · ٢٨`
- Headline line 1: `النتيجة بلا مصدر`
- Headline line 2: `ليست معرفة`
- Footnote: `١ — سجّل مصدر البيانات، إصدارها، وتاريخ تحديثها.`

## Content grounding

NIST AI 600-1 recommends practices for determining data origin and content lineage and documenting upstream sources for provenance. The Arabic is independently authored and intentionally narrower than a compliance claim.

## Concept tournament

1. **Selected — editorial footnote:** the source marker is both reading device and concept; the note supplies the operational behavior.
2. Rejected — transparent data core: another premium object would repeat the feed's still-life behavior.
3. Rejected — lineage diagram: another route or grid would repeat process and metric posts.
4. Rejected — archive dossier: administrative cosplay would obscure the simple provenance lesson.

## Composition constraints

- Native HTML/CSS only; no generated image.
- Asymmetric pale editorial field with a quiet side margin and one footnote rule.
- The Arabic numeral `١` behaves as a real superscript marker, not a decorative giant number.
- No mock page, object, person, chart, route, grid, interface, borrowed texture, generated text, or external asset.

## Quality target

All critics ≥145/160; Arabic, source fidelity, hierarchy, contrast, mobile, originality, and feed gates required; production disabled.
