# Iteration 04 — RTL Process Field Refinement

## Trigger

Iteration 03 failed because its central arrow contradicted the intended Arabic state transition. This pass corrects the semantics and reduces visual noise without changing the lesson.

## Exact copy

- Metadata: `مبدأ تشغيلي · ١٦`
- Headline: `العملية الغامضة` / `لا تصلح للأتمتة`
- Support: `وثّق الخطوات أولًا، ثم أتمت ما يستحق التكرار.`
- Diagram: `قبل التوثيق` / `وضوح` / `بعد التوثيق`

## Targeted changes

1. Arrow now points left, from the ambiguous right-hand field to the documented left-hand grid.
2. Clarity corridor changes from saturated neon to pale `#77FF70` and narrows from 102 px to 70 px.
3. After-state modules use exact repeated widths, heights, and gutters.

## Preserved invariants

Native HTML/CSS only; Ghroob; canonical logo; 2026 brand colors; 1080×1350 plus 324×405; no external pixels; publication disabled.
